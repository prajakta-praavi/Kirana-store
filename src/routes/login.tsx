import { createFileRoute, Link } from "@tanstack/react-router";
import { PageHero } from "@/components/site/PageKit";
export const Route = createFileRoute("/login")({ head: () => ({ meta: [{ title: "Login" }] }), component: () => <Auth title="Login" button="Login securely" footer={<><Link to="/forgot-password" className="text-primary">Forgot password?</Link><span>New customer? </span><Link to="/register" className="text-primary">Register</Link></>} /> });
function Auth({title,button,footer}:{title:string;button:string;footer:React.ReactNode}){return <div><PageHero eyebrow="Secure account" title={title} subtitle="OTP/password-ready authentication UI for a production commerce backend."/><section className="mx-auto max-w-md px-4 py-8"><form className="grid gap-4 rounded-2xl border border-border bg-card p-5 shadow-card"><input className="rounded-xl border border-border bg-secondary px-3 py-3" placeholder="Mobile number or email"/><input className="rounded-xl border border-border bg-secondary px-3 py-3" placeholder="Password / OTP" type="password"/><button className="rounded-xl bg-primary px-4 py-3 font-black text-primary-foreground">{button}</button><div className="flex flex-wrap justify-center gap-2 text-sm text-muted-foreground">{footer}</div></form></section></div>}
