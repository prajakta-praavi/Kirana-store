import { createFileRoute, Link } from "@tanstack/react-router";
import { Package, Heart, MapPin, UserRound } from "lucide-react";
import { PageHero } from "@/components/site/PageKit";
export const Route = createFileRoute("/my-account")({ head: () => ({ meta: [{ title: "My Account" }] }), component: () => <div><PageHero eyebrow="Account" title="My account" subtitle="Manage profile, saved addresses, wishlist, order history and support."/><section className="mx-auto grid max-w-7xl gap-4 px-4 py-8 sm:grid-cols-2 lg:grid-cols-4">{[[UserRound,"Profile","Login or update customer profile","/login"],[Package,"Order History","View invoices, GST and status","/order-history"],[Heart,"Wishlist","Saved products","/wishlist"],[MapPin,"Track Order","Live shipping status","/track-order"]].map(([Icon,title,text,to])=>{const I=Icon as typeof UserRound; return <Link key={title as string} to={to as any} className="rounded-2xl border border-border bg-card p-5 shadow-soft"><I className="size-7 text-primary"/><h2 className="mt-4 font-display text-lg font-black text-ink">{title as string}</h2><p className="mt-2 text-sm text-muted-foreground">{text as string}</p></Link>})}</section></div> });
