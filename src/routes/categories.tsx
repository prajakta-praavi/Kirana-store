import { createFileRoute, Link } from "@tanstack/react-router";
import { PageHero } from "@/components/site/PageKit";
import { categories } from "@/data/products";
export const Route = createFileRoute("/categories")({ head: () => ({ meta: [{ title: "Shop Categories" }, { name: "description", content: "Explore grooming, personal care, home utility and kitchen product categories." }] }), component: CategoriesPage });
function CategoriesPage(){ return <div><PageHero eyebrow="Categories" title="Shop by category" subtitle="Find rechargeable trimmers, clippers, grooming accessories, kitchen tools and home utility products."/><section className="mx-auto grid max-w-7xl grid-cols-2 gap-4 px-4 py-8 sm:grid-cols-3 lg:grid-cols-5">{categories.map((cat)=><Link key={cat.slug} to="/categories/$slug" params={{slug:cat.slug}} className="rounded-2xl border border-border bg-card p-2 shadow-soft transition hover:-translate-y-1 hover:shadow-card"><img src={cat.image} alt={cat.name} className="aspect-[4/3] w-full rounded-xl object-cover"/><div className="p-2"><h2 className="font-display text-sm font-black text-ink">{cat.name}</h2><p className="mt-1 line-clamp-2 text-xs text-muted-foreground">{cat.description}</p></div></Link>)}</section></div> }
