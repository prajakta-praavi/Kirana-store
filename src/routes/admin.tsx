import { createFileRoute } from "@tanstack/react-router";
import { BarChart3, Boxes, Image, Package, ReceiptText, Tags, Users, WalletCards } from "lucide-react";
import { PageHero } from "@/components/site/PageKit";
import { products, categories } from "@/data/products";
export const Route = createFileRoute("/admin")({ head: () => ({ meta: [{ title: "Admin Dashboard" }, { name: "description", content: "Admin panel for products, categories, inventory, orders, customers, coupons, banners, blogs, GST and reports." }] }), component: AdminPage });
function AdminPage(){ const cards = [[Package,"Products",products.length],[Tags,"Categories",categories.length],[Boxes,"Inventory",products.reduce((s,p)=>s+p.stock,0)],[ReceiptText,"Orders",128],[Users,"Customers",964],[WalletCards,"Coupons",12],[Image,"Banners",6],[BarChart3,"Reports",18]]; return <div><PageHero eyebrow="Admin" title="Store management dashboard" subtitle="CMS-ready admin UI for product, category, inventory, order, customer, coupon, banner, blog, GST and analytics management."/><section className="mx-auto max-w-7xl px-4 py-8"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([Icon,label,value])=>{const I=Icon as typeof Package; return <div key={label as string} className="rounded-2xl border border-border bg-card p-5 shadow-soft"><I className="size-7 text-primary"/><div className="mt-4 text-3xl font-black text-ink">{value as number}</div><div className="text-sm font-bold text-muted-foreground">{label as string}</div></div>})}</div><div className="mt-8 rounded-2xl border border-border bg-card p-5 shadow-card"><h2 className="font-display text-xl font-black text-ink">Admin modules</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{["Product Management","Category Management","Inventory Management","Order Management","Customer Management","Coupon Management","Banner Management","Blog Management","Reports & Analytics","GST Management","Payment Integration"].map((item)=><div key={item} className="rounded-xl bg-secondary px-4 py-3 text-sm font-bold text-ink">{item}</div>)}</div></div></section></div> }
