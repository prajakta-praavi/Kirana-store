import { createFileRoute, Link } from "@tanstack/react-router";
import { PageHero } from "@/components/site/PageKit";
export const Route = createFileRoute("/forgot-password")({ head: () => ({ meta: [{ title: "Forgot Password" }] }), component: () => <div><PageHero eyebrow="Account help" title="Forgot password" subtitle="Reset your account using mobile OTP or email."/><section className="mx-auto max-w-md px-4 py-8"><form className="grid gap-4 rounded-2xl border border-border bg-card p-5 shadow-card"><input className="rounded-xl border border-border bg-secondary px-3 py-3" placeholder="Mobile number or email"/><button className="rounded-xl bg-primary px-4 py-3 font-black text-primary-foreground">Send reset OTP</button><Link to="/login" className="text-center text-sm text-primary">Back to login</Link></form></section></div> });
