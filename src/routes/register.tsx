import { createFileRoute, Link } from "@tanstack/react-router";
import { PageHero } from "@/components/site/PageKit";
export const Route = createFileRoute("/register")({ head: () => ({ meta: [{ title: "Register" }] }), component: () => <div><PageHero eyebrow="Create account" title="Register" subtitle="Create a customer account for faster checkout and order tracking."/><section className="mx-auto max-w-md px-4 py-8"><form className="grid gap-4 rounded-2xl border border-border bg-card p-5 shadow-card">{["Full name","Mobile number","Email address","Password"].map((x)=><input key={x} className="rounded-xl border border-border bg-secondary px-3 py-3" placeholder={x} type={x==="Password"?"password":"text"}/>) }<button className="rounded-xl bg-primary px-4 py-3 font-black text-primary-foreground">Create account</button><p className="text-center text-sm text-muted-foreground">Already registered? <Link to="/login" className="text-primary">Login</Link></p></form></section></div> });
